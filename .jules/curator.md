## 2024-05-22 - Co-location of Utility Tests
Structure: Move tests for shared utilities from consuming apps to the package itself.
Rule: Tests for shared packages (like @repo/utils) must be co-located with their source files within the package directory.
